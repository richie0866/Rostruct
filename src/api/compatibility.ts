// Makes an HTTP request
export const httpRequest = request || syn.request || http.request;

// Gets an asset by moving it to Roblox's content folder
export const getContentId = getcustomasset || getsynasset;
